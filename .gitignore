# Swift / SwiftPM build outputs
.build/
build/
.swiftpm/
Package.resolved
DerivedData/
xcuserdata/

# Generated / vendored binaries
Frameworks/
*.xcframework

# xcodegen-generated iOS app project (regenerate via `make ios-project`)
*.xcodeproj

# Signing & local credentials (never commit)
sign
.dev-identity
*.provisionprofile
.spike.local.json

# Environment
.env
.env*.local

# IDEs and editors
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.swp
*.swo
*~
.DS_Store

# Logs
*.log

# Agent / tooling local state
.claude/.last-make-choice
.claude/worktrees/
.worktrees/
.superpowers/

# Local-only scratch space (test keys, throwaway fixtures)
ignore/

# Misc
*.tgz
.cache
tmp
temp
coverage

# Sparkle EdDSA private key export (public key lives in Scripts/sparkle_public_key.txt)
sign/sparkle_ed_private_key*
.testloop/
