{
  "scripts": {
    "setup": "ln -sf AGENTS.md CLAUDE.md && cp $CONDUCTOR_ROOT_PATH/apps/server/.env apps/server/ && rm -rf apps/macos/Frameworks/GhosttyKit.xcframework && cp -R $CONDUCTOR_ROOT_PATH/apps/macos/Frameworks/GhosttyKit.xcframework apps/macos/Frameworks/ && bun i",
    "run": ""
  }
}
