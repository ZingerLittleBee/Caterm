import SwiftUI

public struct SyncTabPlaceholderView: View {
    public init() {}
    public var body: some View {
        Text("Sync settings (integrated in Task 27)").foregroundStyle(.secondary)
    }
}
